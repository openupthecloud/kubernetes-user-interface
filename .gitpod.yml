tasks:
  - name: backend
    command: |
      cd backend
      npm install
      node node.js

  - name: backend
    command: |
      ./scripts/aws-auth.sh
      cd my-app 
      npm install
      npm run start

ports:
  - port: 3000
    onOpen: open-browser
    visibility: private

  - port: 3001
    onOpen: open-browser
    visibility: private
